# exoplanet-sliders
Interactive outreach tools for exoplanet data modelling

The code uses `bokeh` to display a graph of transit and radial velocity curves where model parameter are controlled by sliders. Start the interactive tools by running `bokeh serve --show <filename>` in the terminal.
